[project]
name = "grid-core"
version = "0.1.0"
description = "The Grid runtime core (CK Ops)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
